import { AddNotificationTable } from './add-notification-table';

describe('AddNotificationTable', () => {
  it('should create an instance', () => {
    expect(new AddNotificationTable()).toBeTruthy();
  });
});
